package com.iberdrola.practicas2026.davidsc.core.utils

object AppConfig {
    var useMockLocal = false
    var mockStreet: String? = null // Calle que queremos mostrar en mock si null muestra todas
}